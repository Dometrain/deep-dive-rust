//! Standalone teaching snippets, one file per *Deep Dive: Rust* module.
//!
//! This codebase accumulates across the whole course -- each module's
//! `examples::module_N` stays in place so later modules can refer back to
//! earlier ones (see `module_2::lifetime_bounds_in_traits` referencing
//! `module_1::refactor_todo`, for example). Only ever add a new file here;
//! don't edit a previous module's file when building the next one.

pub mod module_4;
